# 지워진 코드 검색엔진 — 프로젝트 헌장 (Project Charter)

> **이 문서의 목적**
> 이 프로젝트에 관한 모든 것을 한 곳에 모은 문서. 팀원, 교수님, 산업체 멘토, 그리고 각 팀원의 Claude가 이 문서 하나만 읽으면 프로젝트의 배경·목표·설계·일정·규칙을 같은 수준으로 이해할 수 있어야 한다.
>
> **Claude에게:** 이 문서는 팀의 합의 사항이다. 여기 정의된 범위·용어·설계 결정·협업 규칙을 따르고, 여기 없는 결정은 "미결정"으로 취급해 팀원에게 확인을 권한다. 범위를 임의로 확장하지 말고, 스키마·분류 체계·인터페이스를 바꾸는 제안은 반드시 "변경 제안"으로 표시한다.
>
> 버전: v1.6 / 작성일: 2026-09-09 / 다음 갱신: 마감 정확한 날짜 확인 후 (§9 일정 재조정)

---

## 0. 한 페이지 요약

**한 문장:** 오픈소스에서 삭제된 코드만 모아, "왜 지워졌는지"와 "대신 무엇이 들어갔는지"를 함께 검색하는 엔진.

**왜:** 모든 코드 검색과 AI 코딩 도구는 살아남은 코드만 다룬다. "이 방식은 해봤는데 이래서 버렸다"는 지식은 커밋 히스토리에 묻혀 사라진다. 성공은 검색되는데 실패는 삭제된다 — 우리는 삭제된 걸 검색한다.

**무엇을 만드나 (핵심 4 + 확장 4):**
- 핵심: ① 삭제 코드 채굴 파이프라인 ② 의미 있는 삭제 필터 ③ 삭제 이유 분류 ④ 공개 데이터셋
- 확장: ⑤ 의미 검색 + 웹 UI ⑥ 코드 진단(유사 삭제 이력) ⑦ 개인 레포 이유 추정 모델 ⑧ VS Code 플러그인

**어떻게 평가하나:** 필터 정밀도 / 분류 정확도(기준선 비교) / 검색 관련성(기준선 비교) / 사용자 테스트 / 시스템 성능

**일정:** 2026-09 ~ 2026-11 (약 10주). 첫 2주 안에 "이유 회수율" 측정 후 방향 확정.

**교수님 피드백 (2026-09-08):** "주제는 매우 좋아 보입니다. 그러나 구현 범위가 너무 넓어 조금 줄이는 것이 좋을 듯 합니다. 그리고 구현 시스템의 평가를 어떻게 할지도 고민해 보세요."

---

## 1. 배경

### 1.1 과목
- 과목명: 실무중심산학협력프로젝트2 (캡스톤디자인-SW), 3학점, 설계 3시간
- 담당: 박규식 교수 (AI융합대학 소프트웨어학과, 미디어센터 414, kspark@dankook.ac.kr)
- 성격: 4학년 2학기 졸업작품 과목. 3인 팀, 한 학기 동안 SW공학 기반으로 설계·구현·평가·문서화
- 평가: 엄격. GitHub 저장소를 직접 검토. 발표 + 제출물. 성능 지표는 여러 입증 방식 중 하나(필수 아님)
- 강의계획서 핵심 요구: (1) 현실적 제약조건을 반영한 설계·개발 계획 (2) 계획대로 구현 (3) 설계~구현 과정을 체계화한 문서 (4) 성능분석 결과 디스플레이 (5) 목표 달성을 입증하는 논리적·수리적 유도
- 산업체 전문가 멘토링 연계

### 1.2 팀
- 3인, 전원 소프트웨어학과 3학년 (4학년 과목 선수강, 평가는 동일 기준)
- 팀원: 성제 (조장), 재헌, 희수
- 전원 Claude 사용. 협업 채널: Discord (회의), 카카오톡 (공지)
- 마감: 잠정 11/7 (제출·발표 정확한 날짜는 확인 후 갱신 → §17)

### 1.3 주제 선정 경위 (요약)
약 50개 후보를 검토했다. 선정 원칙은 다음과 같았고, 이 원칙은 앞으로의 범위 결정에도 적용된다.
- **아이디어의 참신함이 최우선.** AI 덕분에 구현 장벽이 낮아졌으므로 "무엇을 만드는가"가 차별점
- **어려움은 진짜여야 한다.** 화면 늘리기가 아니라 설계 판단이 계속 필요한 것
- **AI가 대신 못 하는 부분이 있어야 한다.** 깃허브를 뜯어보는 심사에서 "라이브러리 호출 + UI"는 티가 난다
- **완주 가능해야 한다.** 실패 모드가 "축소"이지 "붕괴"가 아닌 것
- **선행 사례 확인 필수.** 유사 서비스가 이미 있으면 차별화 부담이 급증한다 (예: 보행 경로 아이디어는 '그늘로' 앱 발견 후 탈락)

최종 후보 4개(가구 배치 제약 해결기 / 정책 영향 계산기 / 지워진 코드 검색엔진 / 친구 백업) 중 팀 투표로 본 주제 확정 (2026-09-08).

---

## 2. 프로젝트 정의

### 2.1 문제
개발 지식의 절반은 "무엇을 하지 말아야 하나"인데, 그 지식은 어디에도 저장되지 않는다.
- 스택오버플로는 "어떻게"에 답한다
- 코드 검색(GitHub, Sourcegraph)은 현재 상태만 색인한다
- AI 코딩 도구는 살아남은 코드로 학습했다. 그럴싸하지만 나중에 지워질 코드를 뱉을 수 있다
- 삭제 이유는 커밋 메시지·PR·리뷰에 흩어져 있고, 저장소 하나에서도 손으로 찾기 비현실적이다

### 2.2 우리 답
수백 개 오픈소스 저장소의 삭제 이력을 **함수 단위**로 추출하고, **삭제 이유**와 **대체 코드**를 묶어 검색 가능하게 만든다.

### 2.3 기존 것들과의 차이 (선행 조사 결과, 2026-09-07)
| 기존 | 무엇 | 우리와의 차이 |
|---|---|---|
| Sourcegraph diff 검색 | 여러 저장소 커밋 diff를 정규식으로 검색, 삭제 라인 필터 가능 | 문자열 매칭. 이유 없음, 대체 코드 없음, 함수 단위 아님, 의미 검색 없음, 통계 없음 |
| fossil-code (2026-06) | 단일 저장소에서 죽은 코드를 찾고 언제·왜 죽었는지 설명하는 CLI | **내 저장소 청소** 도구. 우리는 **남의 저장소 500개에서 배우는** 검색엔진 |
| deadcode-archaeologist | 위와 같은 단일 저장소 CLI | 동일 |
| MSR 학술 연구 | 리팩토링 동기, 폐기 API 제거, 코드 스멜 제거 이유 등 통계 | 논문이지 도구 아님. 우리 이유 분류 체계의 학술 근거로 인용 |
| Software Heritage | 전 세계 소스코드 아카이브 | 아카이브지 삭제 이유 검색이 아님 |

**존재하지 않음이 확인된 것:** "삭제 함수 + 이유 + 대체 코드" 데이터셋 / 크로스 저장소 삭제 코드 의미 검색 / 삭제 이력 기반 코드 진단

**발표용 차별화 한 줄:** "기존 도구는 내 코드베이스의 죽은 코드를 청소합니다. 기존 검색은 지워진 문자열을 찾습니다. 우리는 오픈소스 전체에서 남들이 왜 지웠는지를 배웁니다."

### 2.4 비유
깃허브가 "살아있는 코드의 도서관"이라면, 이건 "실패한 코드의 부검 기록"이다. 의사가 사망 원인 통계로 배우듯, 개발자가 삭제 원인으로 배운다.

### 2.5 사용자와 사용 장면
**주 사용자:** 시니어 리뷰어가 없는 개발자 — 개인, 소규모 팀, 프리랜서, 학생. (우리 팀이 정확히 이 타깃)

**장면:**
1. 재시도 로직을 직접 짜려는 개발자가 `retry backoff` 검색 → 유명 프로젝트 12곳이 같은 방식을 썼다가 "경쟁 조건"으로 지우고 라이브러리로 교체한 이력 확인
2. PR 올리기 전 코드를 붙여넣기 → "유사 코드 40건 삭제됨, 이유 1위 메모리 누수" (확장 ⑥)
3. AI가 뽑아준 코드 검사 → 그 패턴이 나중에 왜 지워지는지 확인
4. 연구자가 "파이썬 프로젝트의 코드 삭제 이유 분포" 데이터셋을 논문에 사용
5. 커밋 메시지가 부실한 개인 레포에서 "이 삭제는 아마 버그 수정(72%)" 추정 (확장 ⑦)

### 2.6 데이터의 원천과 적용 대상 (중요한 구분)
- **원천(배우는 곳):** PR·이슈 문화가 강한 대형 오픈소스. 여기에만 이유가 기록되어 있다
- **적용 대상(쓰는 곳):** 누구나 — 개인 레포, 팀플, 프리랜서 협업. 여기엔 이유가 없지만 오픈소스가 대신 이유를 준다
- 개인 레포를 원천으로 쓰면 프로젝트가 성립하지 않는다 (커밋 메시지 "asdf"). 이 구분을 발표에서 명확히 한다

---

## 3. 목표

### 3.1 궁극 목표 (끝까지 가면 되는 모습)
소프트웨어 실패 지식의 검색 인프라. 모든 주요 언어·저장소를 색인하고, IDE에서 코드를 쓰는 순간 "이 패턴은 과거 N번 이런 이유로 삭제됨"을 알려주는 조기 경고 시스템. 이유가 기록된 오픈소스에서 삭제의 패턴을 배워, 이유가 기록되지 않은 모든 코드베이스에 그 이유를 되돌려준다.

### 3.2 11월 MVP (반드시 완성)
- 대상: Python, GitHub 상위 저장소 (초기 500개 목표, 실험 후 조정)
- 산출물:
  - 의미 있는 삭제 5만 건 이상 데이터셋 (함수 단위, 이유 분류, 대체 코드, 출처 링크)
  - 삭제 이유 분류기 + 정확도 보고
  - 통계 대시보드 ("삭제 이유 분포", "저장소별 이유 회수율" 등 헤드라인 숫자)
  - 데이터셋·코드 공개 (GitHub + 라이선스 명시)

### 3.3 확장 (핵심이 안정된 뒤, 우선순위 순)
1. 의미 검색 + 웹 검색 UI
2. 코드 진단 API ("이 코드와 유사한 삭제 이력")
3. 개인 레포 이유 추정 모델 (오픈소스 라벨로 학습한 diff→이유 분류기를 메시지 없는 레포에 적용)
4. VS Code 플러그인
5. 두 번째 언어 어댑터 (JavaScript/TypeScript)

**교수님 피드백 반영:** 핵심 4개를 먼저 끝내고, 확장은 앞의 것이 완성된 뒤에만 착수한다. 확장 항목은 발표 "향후 계획"으로 남겨도 프로젝트는 성립한다.

### 3.4 범위 밖 (하지 않는 것)
- 실시간 저장소 모니터링·자동 갱신
- Python 외 언어의 완전 지원 (어댑터 데모까지만)
- 삭제 이유의 세부 원인 추론 ("왜 버그였나"까지는 하지 않음, 대분류까지)
- 클라우드 배포·운영 (로컬/학교 서버 데모로 충분)

---

## 4. 시스템 아키텍처

### 4.1 전체 흐름
```
[저장소 선정 목록]
      ↓
[① 채굴 워커 (병렬)] — 클론 → 전 커밋 순회 → diff에서 삭제 헝크 추출
      ↓
[파서 계층 (언어별 어댑터)] — AST로 함수 경계 식별 → 삭제 함수 단위 레코드
      ↓
[② 의미 있는 삭제 필터] — 이동·리네임·포맷·주석·전체파일삭제 제거
      ↓
[맥락 결합] — 커밋 메시지 + PR 본문 + 연결 이슈 + 리뷰 코멘트 + 대체 코드 후보
      ↓
[③ 이유 분류] — 규칙 + 모델 → 이유(8종) + 근거 등급(명시/추론/불명) + 신뢰도
      ↓
[저장소: PostgreSQL] ──→ [④ 데이터셋 내보내기 (JSONL/Parquet)]
      ↓
[코드 임베딩 → pgvector 인덱스] ──→ [⑤ 검색 API / 웹 UI] [⑥ 진단 API] [통계 대시보드]
```

### 4.2 구성요소별 상세

#### ① 채굴 파이프라인
- 입력: 저장소 URL 목록 (§4.3 선정 기준)
- 동작: 얕은 클론이 아닌 전체 히스토리 클론 → 기본 브랜치의 커밋을 시간순 순회 → 각 커밋의 부모 대비 diff → 삭제된 라인 블록(헝크) 추출
- 병합 커밋 처리: 병합 커밋 자체는 건너뛰고 원 커밋 기준으로 계산 (중복 방지)
- 출력: `(repo, commit_sha, parent_sha, file_path, deleted_hunk, added_hunk_same_file, author_date, commit_message)`
- 병렬화: 저장소 단위로 워커 분배. 실패한 저장소는 재시도 큐
- 저장소당 처리 시간·실패율을 기록 (§10 시스템 평가용)

#### 파서 계층 (언어 독립성의 핵심)
- 인터페이스: `extract_functions(source_text) -> [Function(name, start_line, end_line, body, signature)]`
- 구현체: `PythonAdapter` (tree-sitter-python). 1주차에 인터페이스부터 분리한다
- 삭제 헝크가 함수 경계와 겹치면 "함수 삭제"로, 함수 일부만이면 "부분 삭제"로 태깅. MVP는 **함수 전체 삭제**를 1차 대상으로, 부분 삭제는 확장
- 정규화: 변수명·리터럴을 플레이스홀더로 치환한 정규화 본문을 별도 저장 (이동·클론 탐지용)

#### ② 의미 있는 삭제 필터
"배울 게 있는 삭제"만 남긴다. 다음은 **노이즈로 제외**:
- 이동: 같은 커밋에서 정규화 본문 유사도 ≥ 0.9인 함수가 다른 파일/위치에 추가됨
- 리네임: 본문 동일, 이름만 변경
- 포맷·주석·독스트링만 변경
- 파일 전체 삭제이면서 커밋 메시지에 "remove/delete directory/module" 계열 + 함수 100개 이상 (프로젝트 구조 변경)
- 생성 코드(마이그레이션, 자동 생성 파일, vendored 코드) — 경로 패턴으로 제외
- 테스트 코드 삭제는 별도 플래그로 보존 (제외하지 않되 구분)

필터 규칙은 `filter_rules.md`에 버전 관리하고, 변경 시 정밀도를 재측정한다.

#### 맥락 결합
- 커밋 메시지 (항상)
- PR: 커밋이 속한 PR 번호를 GitHub API로 조회 → 제목·본문·라벨
- 이슈: 커밋/PR 본문의 `#번호`, `fixes #`, `closes #` 참조 → 이슈 제목·본문
- 리뷰 코멘트: PR 리뷰 중 해당 파일·라인 근처 코멘트
- 대체 코드 후보: 같은 커밋·같은 파일에서 삭제 위치 ±N줄 내 추가된 함수/블록. 호출자 변경(삭제 함수를 부르던 곳이 다른 함수를 부르게 됨)도 대체 신호
- GitHub API 한도(인증 시 5,000회/시간) 고려: 저장소 클론은 API 불필요, PR·이슈만 API. 캐시 필수

#### ③ 이유 분류
**이유 8종 (v1, 변경 시 라벨 데이터 재검토 필요):**
| 코드 | 이유 | 정의 | 주요 단서 |
|---|---|---|---|
| BUG | 버그 수정 | 잘못된 동작을 고치기 위해 제거 | "fix", 이슈 참조, 테스트 추가, 조건문 변경 |
| PERF | 성능 | 느리거나 자원을 많이 써서 교체 | "perf", "slow", 벤치마크 언급, 알고리즘 교체 |
| SEC | 보안 | 취약점·위험한 패턴 제거 | "CVE", "security", "injection", 검증 코드 추가 |
| LIB | 라이브러리 교체 | 직접 구현을 외부/표준 라이브러리로 대체 | import 추가, 대체 코드가 라이브러리 호출 |
| DEAD | 죽은 코드 | 호출되지 않아 제거 | 호출자 없음, "unused", "dead" |
| DESIGN | 설계 변경 | 구조·추상화 변경으로 제거 | "refactor", 여러 파일 동시 변경, 클래스 재구성 |
| FEAT | 기능 제거 | 기능 자체를 없앰 (폐기, 지원 종료) | "deprecate", "remove support", 문서 변경 |
| UNK | 불명 | 위 어느 것으로도 판단 불가 | — |

**근거 등급 (모든 레코드에 필수):**
- `EXPLICIT`: PR·이슈·커밋에 이유가 문장으로 있음 → 원문 인용 저장
- `INFERRED`: 대체 코드·테스트·호출 변화로 추론 → 추론 근거 저장, 신뢰도 0~1
- `UNKNOWN`: 판단 불가

**분류 방법 (3단 비교, §10 평가에 사용):**
- 기준선 A: 커밋 메시지 키워드 규칙
- 기준선 B: LLM에 diff + 메시지를 주고 8종 중 선택
- 우리 방식: 맥락 결합 데이터 + 규칙 + 소형 분류기(또는 LLM 앙상블), 근거 등급 산출

**LLM 사용 경계:** LLM은 분류 후보 생성과 추론 근거 문장 작성에만 쓴다. 최종 라벨 정확도는 수동 라벨 500건으로 측정하고, LLM 출력은 검증 없이 정답으로 쓰지 않는다.

#### ④ 데이터셋
- 형식: JSONL (1줄 1레코드) + Parquet. 스키마는 §4.4
- 라이선스: 데이터셋 자체는 CC BY 4.0. 코드 스니펫 포함이므로 permissive 라이선스(MIT/Apache/BSD) 저장소만 포함하고 각 레코드에 원 저장소 라이선스·커밋 링크 표기. GPL 계열은 링크만 (ADR-011)
- 공개 시 README에 수집 방법·필터 규칙·분류 체계·정확도·한계를 명시
- 라벨링한 500건은 별도 파일로 공개 (검증 데이터셋)

#### ⑤ 검색 (확장 1)
- 코드 임베딩: 코드-자연어 공동 임베딩 모델 사용 (후보 §7)
- 인덱스: pgvector
- 질의: 자연어 또는 코드 스니펫 → 임베딩 → 상위 K + 이유·대체 코드 함께 반환
- 필터: 이유 종류, 근거 등급, 저장소, 기간
- 하이브리드: 임베딩 유사도 + 이유 텍스트 키워드 결합 (실험으로 결정)

#### ⑥ 진단 API (확장 2)
- 입력: 코드 스니펫 → 유사 삭제 레코드 상위 N → 이유 분포 요약 ("40건 유사, BUG 55%, LIB 30%")

#### ⑦ 개인 레포 이유 추정 (확장 3)
- 오픈소스에서 학습한 "diff 신호 → 이유" 모델을 커밋 메시지 없는 레포에 적용
- 출력은 항상 `INFERRED`로 표시, 신뢰도 동반
- 대분류까지만. 세부 원인은 범위 밖

#### 통계 대시보드
- 저장소 수, 총 삭제 함수, 필터 후 건수, 이유 분포, 근거 등급 분포, 저장소별 이유 회수율, 연도별 추이
- 강의계획서의 "성능분석 결과 디스플레이" 요구를 여기서 충족

### 4.3 저장소 선정 기준
별 수만으로 고르지 않는다. 다음 기준의 조합:
1. 언어: Python 주 언어
2. 라이선스: MIT / Apache-2.0 / BSD (데이터셋 재배포 가능)
3. PR 문화: 최근 1년 커밋의 70% 이상이 PR 경유
4. 이슈 참조 관행: 커밋/PR에 `#번호` 참조 비율 높음
5. 규모: 커밋 1,000개 이상
6. 첫 실험(§9 1~2주차) 후 저장소별 "이유 회수율"을 측정해 기준을 재조정

초기 후보 예시: django, requests, flask, fastapi, httpx, pydantic, pandas, scikit-learn, celery, sqlalchemy 등 (킥오프에서 20개 확정)

### 4.4 데이터 스키마 (v1)
```
DeletionRecord
  id: uuid
  repo: str                    # owner/name
  repo_license: str
  commit_sha: str
  parent_sha: str
  author_date: datetime
  file_path: str
  language: str                # "python"
  function_name: str
  function_signature: str
  deleted_body: str            # 원문
  deleted_body_normalized: str # 변수명·리터럴 치환
  deletion_kind: enum          # FULL_FUNCTION | PARTIAL
  is_test_code: bool
  filter_status: enum          # KEPT | NOISE_MOVE | NOISE_RENAME | NOISE_FORMAT | NOISE_BULK | NOISE_GENERATED
  filter_rule_version: str
  context:
    commit_message: str
    pr_number: int | null
    pr_title: str | null
    pr_body: str | null
    issue_numbers: [int]
    issue_titles: [str]
    review_comments: [str]
  replacement:
    code: str | null
    match_method: enum         # SAME_LOCATION | CALLER_CHANGE | NONE
    confidence: float
  reason:
    label: enum                # BUG|PERF|SEC|LIB|DEAD|DESIGN|FEAT|UNK
    evidence_grade: enum       # EXPLICIT|INFERRED|UNKNOWN
    evidence_text: str | null  # 원문 인용 또는 추론 근거
    confidence: float
    classifier_version: str
  embedding: vector | null
  source_url: str              # GitHub 커밋 링크
```
스키마 변경은 §13 변경 절차를 따른다.

---

## 5. 핵심 설계 결정 (ADR 요약)

각 결정은 `docs/adr/`에 개별 파일로도 기록한다.

| # | 결정 | 이유 | 대안 |
|---|---|---|---|
| ADR-001 | Python을 첫 언어로 | AST 도구 성숙, 팀 익숙, 상위 프로젝트 PR 문화 강함 | JS/TS (파서 복잡), 다국어 동시 (범위 초과) |
| ADR-002 | 파서 계층을 인터페이스로 분리 | 최종 목표가 다국어. 확장 비용을 어댑터 하나로 | Python 전용 하드코딩 |
| ADR-003 | 함수 전체 삭제를 1차 대상 | 경계가 명확해 필터·매칭이 정확. 부분 삭제는 노이즈 많음 | 라인 단위 (노이즈), 파일 단위 (너무 거침) |
| ADR-004 | 이유 8종 + 근거 3등급 | 100% 이유 회수가 불가능하므로 정직한 표시가 필수. 불명을 숨기지 않음 | 이유만 표시 (신뢰 문제) |
| ADR-005 | LLM은 후보 생성·근거 작성에만 | 정확도는 수동 라벨로 측정. LLM 출력을 정답으로 쓰면 평가가 무의미 | LLM 단독 분류 (기준선 B로만 사용) |
| ADR-006 | 저장소 선정은 PR 문화 기준 | 별 수와 이유 회수율은 무관. 데이터 품질이 프로젝트 생존 조건 | 별 수 상위 N |
| ADR-007 | permissive 라이선스만 데이터셋 포함 | 코드 스니펫 재배포 문제 회피 | 전체 포함 (법적 위험) |
| ADR-008 | 핵심 4 → 확장 순차 진행 | 교수님 피드백. 조각 8개 동시 진행은 캡스톤 실패 패턴 | 전체 병렬 |
| ADR-009 | PostgreSQL + pgvector | 관계형 메타 + 벡터 검색을 한 곳에. 운영 단순 | Elasticsearch, 전용 벡터DB (운영 부담) |
| ADR-010 | 우리 저장소도 PR·이슈 필수 | 도그푸딩(우리 레포에 우리 도구 적용) + 깃허브 심사 대비 | 자유 커밋 |
| ADR-011 | 코드 MIT / 데이터셋 CC BY 4.0 / 스니펫은 원 라이선스 유지 | 데이터셋 재사용을 넓히되 원 저작자 표시 의무 유지. 연구 데이터셋 관행 | 전부 MIT (스니펫 라이선스 충돌), CC0 (표시 의무 없어 원 저작자 보호 약함) |

---

## 6. 팀 운영과 역할

### 6.1 역할 배정

| 팀원 | 역할 | 담당 영역 | 첫 2주 과제 |
|---|---|---|---|
| **성제 (조장)** | **프로덕트·평가 + 조장** | 저장소 선정 스크립트, 통계 대시보드, 웹 UI, 진단 API, 플러그인, 라벨링 총괄·교차 검증 조율, 평가 계획 실행, 회의록·ADR·과목 제출 문서, 발표 | 저장소 20개 선정 기준 스크립트, 라벨 가이드 v1, 문서 템플릿·CI·이슈 보드 세팅 |
| **재헌** | **파이프라인** | 클론·커밋 순회, 파서 계층(인터페이스 + Python 어댑터), 의미 있는 삭제 필터, AST 정규화, 병렬화·실패 복구, DB 스키마 구현 | 저장소 20개 파이프라인 관통, 삭제 유형 분포 조사 |
| **희수** | **분류·검색** | 맥락 결합(GitHub API·PR·이슈·리뷰), 대체 코드 매칭, 이유 분류기, 기준선 A/B 구현·비교, 임베딩·검색 인덱스 | 맥락 결합 프로토타입, 200건 예비 라벨링 주도 → 이유 회수율 측정 (게이트 1) |

**배정 근거**
- 조장이 문서·평가·발표·조율처럼 미루기 쉬운 일을 맡는다. 이 일들은 깃허브 심사와 제출물 품질을 직접 결정하는데, 담당이 없으면 마지막 주에 몰린다
- 재헌: 제안했던 주제(경로 탐색, 제약 해결)가 시스템·알고리즘 성향 → git 내부와 AST를 파는 파이프라인에 맞음
- 희수: 제안했던 주제(키스트로크 ML, 손 추적 분류)가 데이터·모델 성향 → 라벨링·분류기·검색에 맞음
- 킥오프에서 본인이 원하면 재헌↔희수 교환 가능. 조장 역할은 고정

**조장 추가 책임**
- 매주 회의 소집·진행, 회의록 당일 기록
- 게이트(§9) 판정 회의 주재, 결과 기록
- 범위 팽창 감시 (ADR-008)
- 교수님·산업체 멘토 연락 창구
- PR 리뷰 병목 시 최종 리뷰어

**공통 원칙**
- 역할은 주 담당이지 독점이 아니다. 막히면 다른 영역을 돕는다
- 라벨링 500건은 셋이 나눠서 하고 교차 검증한다 (조장이 배분·일정 관리)
- 각자 담당 영역의 인터페이스(파서 계층, 스키마, API 스펙)를 문서화해서 다른 팀원의 Claude가 참조할 수 있게 한다

### 6.2 회의
- 주 1회 정기 (Discord, 화면 공유). 안건: 지난주 완료 / 이번주 계획 / 막힌 것 / 결정 필요 사항
- 회의록은 `docs/meetings/YYYY-MM-DD.md`에 당일 기록. 결정 사항은 ADR로 승격
- 긴급 사항은 카톡, 논의는 Discord 스레드

### 6.3 커뮤니케이션 원칙
- 솔직한 평가 우선. 약한 부분은 돌려 말하지 않는다
- 범위 확장 제안은 환영하되 "핵심 4개 완료 후"가 기본 답
- 결정은 기록 없으면 없는 것

---

## 7. 기술 스택과 환경

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Python 3.12 | 파이프라인·분류·API |
| git 접근 | pygit2 (libgit2) | GitPython보다 빠름. 대안: 직접 `git` CLI 호출 |
| AST | tree-sitter + tree-sitter-python | 다국어 확장 대비 |
| DB | PostgreSQL 16 + pgvector | Docker Compose로 로컬 구동 |
| 임베딩 | 코드-자연어 공동 임베딩 모델 (킥오프에서 후보 2~3개 비교 후 확정) | 로컬 실행 가능한 것 우선 |
| 분류기 | 규칙 + scikit-learn 또는 소형 트랜스포머 파인튜닝 + LLM API 앙상블 | §4.2 ③ |
| API | FastAPI | |
| 프론트 | React + Vite | 대시보드·검색 UI |
| 플러그인 | VS Code Extension API (TypeScript) | 확장 4 |
| 병렬 | multiprocessing 또는 간단한 작업 큐 | 저장소 단위 |
| 테스트 | pytest | 필터 규칙은 반드시 테스트 케이스 동반 |
| 문서 | Markdown, Mermaid | `docs/` |
| CI | GitHub Actions | 테스트 + 린트 |

**실행 환경 (확정)**
- 서버·GPU 없음. 팀원 노트북 3대가 전부. 파이프라인은 CPU 작업이므로 문제없음
- 저장소 클론은 셋이 나눠서 (저장소 목록을 3분할, 각자 로컬에서 추출 후 결과만 공유 DB 또는 JSONL로 합침). 추출이 끝난 저장소는 로컬에서 삭제해 용량 회수
- 용량 추정: 상위 파이썬 저장소 전체 히스토리 평균 100~200MB, 큰 것 500MB+. 500개 ≈ 50~100GB. 1인당 30GB 안팎. DB 수 GB
- 임베딩: CPU에서 도는 소형 모델. 한 번만 계산하므로 느려도 됨
- 분류기: GPU 필요한 파인튜닝은 하지 않음. 규칙 + scikit-learn + LLM API
- **GitHub API 토큰:** 깃허브 계정에서 발급하는 개인 접근 토큰. 없으면 시간당 60회, 있으면 5,000회. PR·이슈 수집에 필수. 발급: GitHub → Settings → Developer settings → Personal access tokens (classic) → `public_repo` 권한만 → 생성된 문자열을 `.env`의 `GITHUB_TOKEN=`에 저장. 절대 커밋 금지 (§8.4). 셋이 각자 발급해 로테이션

---

## 8. 저장소 구조와 개발 규칙

### 8.1 저장소 구조 (제안)
```
deleted-code-search/
  README.md                 # 프로젝트 소개, 실행 방법
  CHARTER.md                # 이 문서
  CLAUDE.md                 # Claude Code용 지침 (§8.6)
  STATUS.md                 # 현재 진행 상태 (§8.7) — 매주 갱신
  docs/
    adr/                    # 설계 결정 기록
    meetings/               # 회의록
    weekly/                 # 주간 진행 로그 (§8.7)
    labeling_guide.md       # 이유 라벨링 가이드
    filter_rules.md         # 필터 규칙 (버전 관리)
    evaluation.md           # 평가 계획·결과
    reports/                # 과목 제출 문서
  pipeline/
    clone.py, walk.py, extract.py, filter.py, context.py
    parsers/
      base.py               # 인터페이스
      python_adapter.py
  classify/
    rules.py, baseline_keyword.py, baseline_llm.py, model.py
  search/
    embed.py, index.py, api.py
  web/                      # React
  datasets/
    labeled_500.jsonl       # 수동 라벨 (검증용)
    export/                 # 공개 데이터셋 빌드 스크립트
  tests/
  docker-compose.yml
```

### 8.2 브랜치·PR
- `main`은 보호. 직접 푸시 금지
- **개인 브랜치 없음. 이슈별 브랜치.** 작업 하나 = 이슈 하나 = 브랜치 하나. 한 사람 브랜치에 작업을 쌓으면 PR이 커지고 충돌이 몰린다
- 브랜치 이름: `{type}/{이슈번호}-{짧은-설명}-{이니셜}` — 예: `feat/12-python-adapter-jh`, `fix/31-move-filter-sj`, `exp/45-embedding-compare-hs`
  - type: feat / fix / refactor / exp(실험) / docs / chore
  - 이니셜: sj(성제), jh(재헌), hs(희수)
- 머지 후 브랜치는 삭제. 오래 사는 브랜치는 `main`뿐
- 브랜치 수명 목표: 1주 이내. 넘어가면 이슈를 쪼갠다
- **모든 변경은 PR 경유, 최소 1명 리뷰.** 이유: 도그푸딩(우리 레포의 삭제에도 이유가 남게) + 깃허브 심사 대비
- PR 템플릿: 무엇을 / 왜 / 어떻게 검증 / 관련 이슈
- 이슈: 작업 단위마다 이슈 생성. 커밋·PR에 `#번호` 참조 필수

### 8.3 커밋 메시지
```
type(scope): 요약 (50자 이내)

본문: 왜 이 변경인가 (무엇이 아니라 왜)
Refs #이슈번호
```
type: feat / fix / refactor / perf / test / docs / chore / exp
**코드를 삭제하는 커밋은 본문에 삭제 이유를 반드시 적는다** — 우리 도구가 우리 레포를 분석할 때 EXPLICIT이 나오도록.

### 8.4 코드 규칙
- 포맷: ruff (format + lint). CI에서 강제
- 타입 힌트 필수 (public 함수)
- 필터 규칙·분류 규칙 변경은 테스트와 정밀도 재측정 결과를 PR에 첨부
- 실험 코드는 `exp/` 브랜치 또는 `notebooks/`에. main에는 재현 가능한 스크립트만
- **비밀 관리:** GitHub 토큰, LLM API 키, DB 비밀번호는 `.env`에만. `.env`는 `.gitignore`, `.env.example`만 커밋. 커밋 전 훅(pre-commit)으로 토큰 패턴 검사. 실수로 올렸으면 즉시 토큰 폐기·재발급 후 히스토리 정리
- **우리 레포 라이선스:** MIT (코드), 데이터셋은 §4.2 ④ 별도 표기

### 8.5 Claude 사용 규칙 (팀 공통)
- 각자 Claude에 이 문서를 먼저 읽힌다. 경로 두 가지:
  - **claude.ai 채팅/프로젝트:** 새 세션 첫 메시지에 `CHARTER.md` 첨부 + "이 문서 기준으로 진행, 나는 [역할] 담당" 한 줄. 프로젝트 기능을 쓰면 프로젝트 지식에 올려두고 세션마다 첨부 생략 가능
  - **Claude Code:** 저장소 루트 `CLAUDE.md`가 자동으로 읽힘. 별도 첨부 불필요 (§8.6)
- Claude가 생성한 코드도 PR 리뷰를 거친다. "Claude가 짰다"는 리뷰 생략 사유가 아니다
- Claude에게 범위 확장을 제안받으면 §3.3 우선순위와 대조한다
- 스키마·분류 체계·인터페이스 변경은 Claude가 아니라 팀이 결정한다 (§13)
- 라벨링은 사람이 한다. Claude는 라벨 후보 제안까지만

### 8.6 Claude Code 규칙

팀 전원이 Claude Code로 개발한다. 다음을 지킨다.

**저장소 루트의 `CLAUDE.md`** — Claude Code가 세션마다 자동으로 읽는 파일. 내용:
- "CHARTER.md를 먼저 읽을 것. 범위·용어·스키마는 CHARTER.md가 기준"
- "STATUS.md를 읽고 현재 어디까지 됐는지 파악한 뒤 작업 시작"
- 실행 방법, 테스트 명령, 린트 명령
- 절대 하지 말 것: main 직접 푸시, 스키마·분류 체계 임의 변경, 테스트 없는 필터 규칙 변경, LLM 출력을 라벨 정답으로 저장
- 담당 영역 경계: 다른 팀원 영역의 파일을 수정할 땐 인터페이스만 건드리고 PR 본문에 명시

**작업 단위**
- 이슈 하나 = 브랜치 하나 = Claude Code 세션 하나가 기본. 세션 시작 시 "이슈 #N 작업"이라고 말하고 시작
- 세션 종료 전 반드시: 테스트 실행 → 커밋 → PR 생성(또는 초안 PR) → STATUS.md 갱신 여부 확인
- 하나의 세션에서 여러 이슈를 섞지 않는다. 컨텍스트가 섞이면 PR 리뷰가 어려워진다

**Claude Code가 만든 코드**
- 사람이 읽고 이해한 뒤 커밋한다. 이해 못 한 코드는 커밋하지 않는다 (심사에서 "이 코드 설명해보세요"가 나온다)
- 커밋 메시지·PR 본문은 사람이 최종 확인. "왜"가 빠져 있으면 채운다
- 대량 파일 변경(10개 이상)은 PR을 나눈다

**충돌 방지**
- 작업 시작 전 `git pull --rebase`로 최신 main 반영
- 같은 파일을 두 사람이 동시에 건드릴 것 같으면 카톡으로 먼저 말한다
- 파서 인터페이스(`pipeline/parsers/base.py`), 스키마(`DeletionRecord`), API 스펙은 "인터페이스 파일"로 지정. 변경은 §13 절차

### 8.7 진행 기록 규칙

"어디까지 했고, 뭐가 됐고, 뭐가 안 됐는지"를 항상 알 수 있게 세 층으로 기록한다.

**1층 — 이슈 보드 (실시간)**
- GitHub Projects 칸반: `Backlog → This Week → In Progress → Review → Done → Blocked`
- 모든 작업은 이슈로 존재. 이슈 없는 작업은 없는 작업
- 이슈 템플릿: 목표 / 완료 조건 / 담당 / 관련 이슈
- `Blocked`로 옮길 땐 무엇에 막혔는지 코멘트 필수

**2층 — `STATUS.md` (주 1회, 회의 직후 조장이 갱신)**
현재 상태를 한 페이지로. 새로 합류한 사람이나 새 Claude 세션이 이것만 읽으면 지금 상황을 아는 문서.
```
# STATUS (갱신: YYYY-MM-DD, N주차)

## 지금 단계
핵심 4개 중 어디 / 게이트 통과 여부

## 완료된 것
- [기능] 한 줄 설명 (PR #번호)

## 진행 중
- [기능] 담당, 예상 완료, 현재 상태

## 안 된 것 / 막힌 것
- [문제] 무엇이 왜 안 되는지, 시도한 것, 다음 시도

## 측정된 숫자 (최신)
- 저장소 수 / 삭제 건수 / 필터 정밀도 / 이유 회수율 / 분류 정확도 …

## 이번 주 결정
- 결정 → ADR 번호

## 다음 주 계획
```

**3층 — 주간 로그 `docs/weekly/W{N}.md` (누적, 삭제하지 않음)**
STATUS.md는 덮어쓰지만 주간 로그는 쌓인다. 최종 보고서와 발표의 "개발 과정" 부분이 여기서 나온다. 내용: 그 주의 목표 / 실제 한 것 / 안 된 것과 이유 / 배운 것 / 숫자 변화 / 다음 주.

**실패 기록 원칙**
- 시도했다가 버린 접근은 반드시 기록한다 (이 프로젝트의 주제가 그것이다). `docs/weekly/`나 ADR에 "시도 → 실패 이유 → 대안"
- 삭제 커밋 메시지에 이유 필수 (§8.3). 우리 레포를 우리 도구로 분석했을 때 EXPLICIT이 나와야 한다

**게이트 기록**
- §9의 게이트 판정은 `docs/meetings/`에 별도 파일로: 측정값, 판정, 근거, 바뀐 계획

**어디에 뭘 기록하나 (요약)**
| 기록 대상 | 위치 | 주기 |
|---|---|---|
| 작업 상태 | GitHub Projects | 실시간 |
| 현재 상황 요약 | STATUS.md | 주 1회 |
| 개발 과정 누적 | docs/weekly/ | 주 1회 |
| 설계 결정 | docs/adr/ | 결정 시 |
| 회의 | docs/meetings/ | 회의마다 |
| 게이트 판정 | docs/meetings/gate-N.md | 게이트마다 |
| 측정 결과 | docs/evaluation.md | 측정마다 |
| 코드 변경 이유 | 커밋 메시지·PR | 커밋마다 |

---

## 9. 일정 (10주)

킥오프를 9월 둘째 주로 가정. 실제 날짜는 킥오프에서 확정.

| 주 | 기간(가정) | 핵심 작업 | 확인 지점 (게이트) |
|---|---|---|---|
| 1 | 9/14~ | 저장소 구조·CI·DB 세팅, 파서 인터페이스 분리, 저장소 20개 선정, 라벨 가이드 v1 | 파이프라인이 저장소 1개를 끝까지 통과 |
| 2 | 9/21~ | 20개 전체 실행, 삭제 유형 분포 조사, **200건 예비 라벨링 → 이유 회수율 측정** | **게이트 1:** EXPLICIT+INFERRED ≥ 60% → 진행 / 40~60% → 저장소 기준 강화 / <40% → 대체 코드 중심으로 축 이동 |
| 3 | 9/28~ | 이동·리네임 필터, AST 정규화, 필터 테스트 | 필터 정밀도 (수동 100건) ≥ 85% |
| 4 | 10/5~ | 저장소 100→500 확장, 병렬화, 실패 복구, GitHub API 캐시 | 500개 처리 완료, 실패율 보고 |
| 5 | 10/12~ | 맥락 결합 완성, 라벨 500건 완료(교차 검증), 기준선 A/B 구현 | 라벨 일치도(kappa) 보고 |
| 6 | 10/19~ | 이유 분류기 학습·평가, 근거 등급 산출, 통계 대시보드 v1 | **게이트 2:** 분류 정확도 ≥ 80% & 기준선 대비 우위 |
| 7 | 10/26~ | 데이터셋 내보내기·README·라이선스 정리, 중간 보고서 | 데이터셋 v0.9 |
| 8 | 11/2~ | (확장) 임베딩·검색 API·웹 UI / 검색 관련성 평가 | Precision@10 보고 |
| 9 | 11/9~ | (확장) 진단 API, 개발자 10명 사용 테스트, 우리 레포 재귀 데모 | 사용 사례 5건 |
| 10 | 11/16~ | 최종 문서, 발표 준비, 데이터셋 공개, 리허설 | 제출 |

**버퍼:** 8~9주차 확장 항목은 6주차 게이트 결과에 따라 축소 가능. 최악의 경우 7주차 산출물(데이터셋 + 통계 + 분류기)만으로 제출한다.

---

## 10. 평가 계획

교수님 피드백("평가를 어떻게 할지 고민")에 대한 답. 각 층은 독립적으로 보고 가능하다.

### 10.1 필터 평가
- 방법: 필터 통과 레코드 100건 + 제외 레코드 100건 무작위 추출. 셋이 독립 판정 후 다수결
- 지표: 정밀도(통과 중 진짜 의미 있음 비율), 재현율(의미 있음 중 통과 비율), 판정자 일치도(Cohen's kappa)
- 목표: 정밀도 ≥ 90%, kappa ≥ 0.7

### 10.2 이유 분류 평가
- 데이터: 수동 라벨 500건 (학습 300 / 검증 100 / 테스트 100). 각 건 2인 이상 라벨, 불일치는 토론으로 확정
- 지표: 정확도, 클래스별 F1, 혼동 행렬, 근거 등급별 정확도
- **기준선 비교 (필수):** 기준선 A(키워드 규칙) / 기준선 B(LLM 직접 질의) / 우리 방식. 우리 방식이 두 기준선보다 나아야 기여가 성립
- 목표: 테스트 정확도 ≥ 80%, 기준선 대비 +10%p 이상

### 10.3 검색 평가 (확장 시)
- 질의 30개 사전 작성 (자연어 15 + 코드 15). 상위 10개 결과를 관련/무관 판정
- 지표: Precision@10, MRR
- 기준선: 키워드 매칭(단순 grep), Sourcegraph 스타일 정규식
- 목표: P@10 ≥ 0.6, 기준선 대비 우위

### 10.4 사용자 평가 (확장 시)
- 개발자 10명(팀 외). 과제: 자기 코드 또는 제공 코드로 검색·진단 → "몰랐던 정보를 얻었는가" 5점 척도 + 자유 기술
- 지표: 평균 점수, "유용한 발견" 사례 수
- 목표: 사례 5건 이상

### 10.5 시스템 평가
- 데이터 규모: 저장소 수, 총 삭제 함수, 필터 후 건수, 이유 분포, 근거 등급 분포, 저장소별 이유 회수율
- 처리 성능: 저장소당 처리 시간, 총 처리 시간, 실패율
- 검색 응답 시간 (≤ 1초 목표)
- 강의계획서 "성능분석 결과 디스플레이" → 대시보드로 충족

### 10.6 논리적 입증 (강의계획서 요구)
"목표 달성 입증"은 다음 논증으로:
1. 의미 있는 삭제를 정밀도 P로 골라냈다 (10.1)
2. 그 삭제의 이유를 정확도 A로 맞혔고, 단순 방법보다 Δ만큼 낫다 (10.2)
3. 따라서 데이터셋의 레코드는 (P × A) 이상의 확률로 "정확한 이유가 붙은 의미 있는 삭제"다
4. 이 데이터셋으로 실제 개발자가 유용한 정보를 얻었다 (10.4)

---

## 11. 리스크와 대응

| 리스크 | 징후 | 대응 | 담당 |
|---|---|---|---|
| **이유 회수율 저조** (최대 리스크) | 2주차 200건에서 UNKNOWN ≥ 60% | 저장소를 PR 문화 기준으로 재선정 → 그래도 낮으면 "대체 코드 검색" 중심으로 축 이동. 프로젝트 첫 문장을 "왜"에서 "대신 뭐가 들어갔나"로 | 분류·검색 |
| 의미 있는 삭제 비율 극히 낮음 | 필터 후 건수가 목표의 20% 미만 | 저장소 1,000개로 확대 / 클래스·모듈 단위 포함 / 부분 삭제 포함 | 파이프라인 |
| 필터 정밀도 미달 | 이동·리네임이 계속 섞임 | 정규화 강화, 유사도 임계 조정, 커밋 내 전역 매칭 | 파이프라인 |
| GitHub API 한도 | PR·이슈 수집 지연 | 팀원 3명 토큰 로테이션, 캐시, PR 정보는 상위 저장소 우선 | 분류·검색 |
| 저장 용량·처리 시간 | 클론 수십 GB, 처리 수일 | 학교 서버 확보, 얕은 클론 불가하므로 저장소 수 조정 | 파이프라인 |
| 라벨 품질 | kappa < 0.6 | 라벨 가이드 개정, 애매 클래스 병합(DESIGN/FEAT 등) | 분류·검색 |
| 데모가 비개발자에게 약함 | 리허설 반응 | 발표 첫 3분을 "실패 지식이 사라지는 문제" 스토리로, 대시보드 숫자 중심 | 프로덕트 |
| 팀 동기 저하 | 진행 정체 | 3주차부터 우리 레포에 도구 적용해 재귀적 재미 확보 | 전원 |
| 유사 도구 등장 | fossil 등 신규 릴리스 | 데이터셋을 7주차에 조기 공개해 "먼저 했다" 기록 | 프로덕트 |
| 범위 팽창 | 확장 항목이 핵심보다 먼저 시작됨 | ADR-008 상기. 회의에서 즉시 중단 | 전원 |

---

## 12. 발표와 데모

### 12.1 발표 구조 (10분 가정)
1. 문제 (2분): "성공은 검색되고 실패는 삭제됩니다." 개발자가 삭제 이유를 찾으려 할 때 겪는 일
2. 우리 답 (1분): 시스템 한 장 그림
3. 헤드라인 숫자 (2분): 저장소 N개, 삭제 함수 M개, 이유 분포, 회수율 — 세상에 없던 숫자
4. 라이브 데모 (3분): 검색 → 이유 → 대체 코드 / 진단 / 우리 레포 재귀
5. 평가 (1분): 필터 정밀도, 분류 정확도 vs 기준선, 사용자 사례
6. 배운 것과 향후 (1분): 다국어 확장, 개인 레포 추정, 플러그인

### 12.2 데모 시나리오
1. 대시보드: "500개 저장소, N만 개 함수, 이유 1위 ○○"
2. 검색 `retry backoff` → 삭제 구현 + 이유 + 대체 코드
3. 심사위원 제공 코드 → "유사 삭제 이력 N건, 주요 이유 ○○"
4. 우리 프로젝트 저장소를 넣어 "우리가 두 달간 지운 코드와 이유"
5. 데이터셋 공개 링크

라이브 데모는 사전 검증된 질의 3개로. 네트워크 없이 돌아가는 로컬 환경 준비.

### 12.3 예상 질문과 답
- "git log 뒤지면 되는 거 아닌가?" → 저장소 하나에서도 비현실적. 500개를 함수 단위로 이유와 함께 검색하는 건 시스템 없이 불가능
- "삭제 이유를 어떻게 아나?" → 커밋·PR·이슈·리뷰 + 대체 코드. 근거 등급으로 정직하게 표시. 정확도와 불명 비율을 숫자로 보고
- "fossil-code / Sourcegraph와 뭐가 다른가?" → §2.3
- "LLM이 다 한 거 아닌가?" → LLM은 후보 생성·근거 작성만. 정확도는 수동 라벨로 측정했고, LLM 직접 질의(기준선 B)보다 우리 방식이 Δ만큼 낫다
- "누가 쓰나?" → 시니어 리뷰어 없는 개발자·팀·학생, 그리고 AI 코딩 시대의 코드 검증. 연구자에겐 데이터셋
- "개인 레포에선 안 되지 않나?" → 원천은 오픈소스, 적용은 누구나. 확장 ⑦이 그 답
- "왜 파이썬만?" → 검증을 위한 첫 언어. 파서 계층 분리로 확장 가능 (어댑터 데모)

---

## 13. 변경 절차

- 스키마(§4.4), 이유 분류 체계(§4.2 ③), 파서 인터페이스, 필터 규칙 버전, 범위(§3)의 변경은 **이슈 → 회의 논의 → ADR 기록 → 이 문서 갱신** 순서
- 이미 라벨링된 데이터에 영향을 주는 변경(분류 체계 등)은 재라벨 비용을 명시하고 결정
- 이 문서의 버전을 올리고 §19 변경 이력에 기록

---

## 14. 과목 제출 문서 계획

강의계획서의 "설계~구현 과정 문서화" 요구에 대응. `docs/reports/`에 관리.
1. 제안서: 배경, 목표, 범위, 일정, 역할 (이 문서 §1~3, §6, §9 기반)
2. 요구사항 명세: 기능·비기능 요구, 사용자 시나리오 (§2.5, §3)
3. 설계서: 아키텍처, 컴포넌트, 데이터 스키마, ADR (§4, §5)
4. 테스트·평가 계획 및 결과 (§10)
5. 중간 보고서 (7주차)
6. 최종 보고서 + 발표 자료 + 데이터셋 README (10주차)

---

## 15. 용어집

| 용어 | 뜻 |
|---|---|
| 삭제 헝크 (deleted hunk) | diff에서 `-`로 표시된 연속 라인 블록 |
| 함수 전체 삭제 | 함수 정의 시작~끝이 모두 삭제된 경우 |
| 의미 있는 삭제 | 이동·리네임·포맷·생성코드·대량 구조 변경을 제외한, 배울 게 있는 삭제 (§4.2 ②) |
| 노이즈 | 위 제외 대상 |
| 대체 코드 (replacement) | 삭제 위치 또는 호출자 변경으로 식별된, 삭제 코드를 대신하는 코드 |
| 이유 (reason) | 8종 분류 (§4.2 ③) |
| 근거 등급 (evidence grade) | EXPLICIT / INFERRED / UNKNOWN |
| 이유 회수율 | 전체 레코드 중 EXPLICIT+INFERRED 비율. 2주차 게이트 지표 |
| 기준선 (baseline) | 우리 방식과 비교할 단순 방법 (키워드 규칙, LLM 직접 질의) |
| 정규화 본문 | 변수명·리터럴을 치환한 함수 본문. 이동·클론 탐지용 |
| 도그푸딩 | 우리 도구를 우리 저장소에 적용 |
| 게이트 | 계속 진행 여부를 결정하는 확인 지점 (§9) |
| ADR | Architecture Decision Record, 설계 결정 기록 |

---

## 16. 각 팀원의 Claude에게 주는 지침

이 문서를 읽은 Claude는 다음을 지킨다.
1. 프로젝트 범위는 §3. 핵심 4개 완료 전에 확장 항목 구현을 제안하지 않는다
2. 용어는 §15, 스키마는 §4.4, 분류 체계는 §4.2 ③을 그대로 쓴다. 다른 이름을 만들지 않는다
3. 필터·분류 규칙을 바꾸는 코드는 테스트와 정밀도 재측정을 함께 요구한다
4. LLM(자기 자신 포함)의 출력을 라벨 정답으로 쓰는 코드는 작성하지 않는다. 후보 제안까지만
5. 삭제 커밋의 메시지에는 이유를 쓰도록 안내한다 (§8.3)
6. 설계 변경이 필요하다고 판단되면 "변경 제안"으로 표시하고 §13 절차를 안내한다
7. 팀원이 다른 팀원 담당 영역을 건드릴 때 인터페이스(§4.2 파서 계층, §4.4 스키마)를 기준으로 충돌 여부를 확인한다
8. 모르는 결정은 §17 미결정 목록을 확인하고, 없으면 팀 확인을 권한다
9. 솔직하게 평가한다. 약한 부분을 돌려 말하지 않는다
10. 세션 시작 시 STATUS.md를 읽고, 세션 종료 전 STATUS.md·이슈 보드 갱신이 필요한지 확인한다 (§8.7)
11. 이슈 하나에 세션 하나. 다른 이슈 작업을 섞자는 요청에는 새 세션을 권한다 (§8.6)

---

## 17. 미결정 사항 (킥오프에서 확정)

- [x] 팀원별 역할 배정 (§6.1) — 배정 유지 확정 (재헌↔희수 교환 없음, 2026-09-09)
- [x] 킥오프 날짜와 정확한 마감일 (제출·발표) — 마감 잠정 11/7 (2026-09-09). 제출·발표 정확한 날짜 확인 후 §9에 실제 날짜 기입
- [ ] 초기 저장소 20개 목록
- [ ] 임베딩 모델 선택 (후보 비교 후, CPU 실행 가능한 소형 모델 우선)
- [ ] 분류기 방식 (규칙+scikit-learn vs LLM API 앙상블) — 6주차 실험으로. GPU 없으므로 트랜스포머 파인튜닝은 제외
- [x] 실행 환경 — 학교 서버·GPU 없음. 팀원 노트북 3대에 저장소를 분배해 병렬 처리 (§7 환경 참조)
- [x] GitHub API 토큰 — 발급 완료 (2026-09-09, 각자 `.env`에만 저장, §7 환경 참조)
- [x] 데이터셋 라이선스 — 코드 MIT / 데이터셋 CC BY 4.0 / 포함 스니펫은 원 저장소 라이선스·출처 링크 유지 (ADR-011)
- 발표 시간·형식, 산업체 멘토 일정: 조장이 그때그때 공지 (문서에서 관리하지 않음)

---

## 18. 시작 체크리스트 (킥오프 전후 순서)

셋이 동시에 시작하면 꼬인다. 아래 순서대로.

**성제 (킥오프 전, 혼자)**
1. GitHub 조직 또는 개인 계정에 저장소 생성 (`deleted-code-search`, private으로 시작, 공개는 7주차)
2. `main` 보호 규칙 설정 (직접 푸시 금지, PR 리뷰 1명 필수)
3. 저장소 골격 커밋: §8.1 구조, `CHARTER.md`, `CLAUDE.md`(§8.6 내용), `STATUS.md`(§8.7 템플릿, "0주차: 세팅 중"), `.gitignore`, `.env.example`, `LICENSE`(MIT), PR·이슈 템플릿, `docker-compose.yml`(PostgreSQL+pgvector), CI 기본(ruff + pytest)
4. GitHub Projects 칸반 생성, §17 미결정 항목을 이슈로 등록
5. 1주차 과제를 역할별 이슈로 등록 (§6.1 "첫 2주 과제" 기준)
6. 재헌·희수를 협업자로 초대

**재헌·희수 (초대 후)**
1. 클론, `docker compose up`으로 DB 뜨는지 확인
2. `CHARTER.md` 정독, 자기 역할의 1주차 이슈 확인
3. GitHub 토큰 발급 (§7 절차) → `.env`에만 저장
4. Claude Code에서 저장소 열고 `CLAUDE.md`가 읽히는지 확인 ("이 프로젝트가 뭐야?"라고 물어 CHARTER 기반으로 답하면 정상)
5. 첫 이슈 브랜치 생성 → 작은 PR 하나 올려 리뷰·머지 흐름 테스트

**킥오프 회의 (셋)**
1. §17 미결정 항목 확정 (날짜, 저장소 20개, 서버, 역할 교환 여부)
2. §9 일정에 실제 날짜 기입, 게이트 1 날짜 확정
3. 회의록 → `docs/meetings/`, 결정 → ADR
4. `STATUS.md`를 "1주차 시작"으로 갱신

**매주 반복 (조장)**
- 회의 → 회의록 → STATUS.md 갱신 → 주간 로그 → 다음 주 이슈 정리

---

## 19. 변경 이력

| 버전 | 날짜 | 변경 | 작성 |
|---|---|---|---|
| v1.0 | 2026-09-08 | 최초 작성. 주제 확정, 교수님 피드백 반영, 핵심/확장 분리, 평가 계획 수립 | 성제 |
| v1.1 | 2026-09-08 | 역할 배정 확정 (§6.1), 조장 책임 추가 | 성제 |
| v1.2 | 2026-09-08 | Claude Code 규칙 (§8.6), 진행 기록 규칙 (§8.7), CLAUDE.md·STATUS.md 추가 | 성제 |
| v1.3 | 2026-09-08 | 브랜치 규칙 상세화 — 이슈별 브랜치, 이름 규칙, 수명 (§8.2) | 성제 |
| v1.4 | 2026-09-08 | 비밀 관리·레포 라이선스 (§8.4), Claude 두 경로 (§8.5), 시작 체크리스트 (§18) 추가 | 성제 |
| v1.5 | 2026-09-08 | 실행 환경 확정(서버·GPU 없음, 노트북 3대 분산), 토큰 발급 절차, 데이터셋 라이선스 확정(ADR-011), §17 정리 | 성제 |
| v1.6 | 2026-09-09 | 역할 유지 확정(재헌↔희수 교환 없음), 마감 잠정 11/7, GitHub 토큰 발급 완료 반영 (§1.2, §17); STATUS.md 1주차 초기화·주간 로그 시작 (#3) | 성제 |
